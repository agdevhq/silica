# Silica Docs

Documentation site for the Silica framework, served from this example vault.

```bash
npm run build
npm run dev
```

The visible project is only markdown and config. The Next.js app is materialized under `.silica/next/`.
