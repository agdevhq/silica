---
"@silicajs/next": minor
---

Generate an `/api/mcp` route when `assistant.mcp` is enabled, let the proxy bypass sign-in for self-authenticating path prefixes, and externalize the MCP SDK in the generated Next config only when the server is on.
