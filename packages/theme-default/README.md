# @silicajs/theme-default

Default Silica theme.

Exports a `{ Layout, PageRenderer }` pair plus `styles.css`. The layout owns persistent chrome (sidebar, header, search trigger, dark-mode toggle) while the page renderer displays the current note, ToC, tags, and backlinks.

Custom themes should follow the same component contract from `@silicajs/next/theme`.
