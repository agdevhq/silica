# @silicajs/cli

Reserved package — the `silica` CLI (`create`, `dev`, `build`, `start`) for the upcoming [Silica](https://www.npmjs.com/package/silicajs) framework.

This `0.0.0` release is a placeholder reserving the name.
