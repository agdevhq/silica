---
"@silicajs/cli": minor
---

Materialize the MCP route and its SDK dependency when `assistant.mcp` is enabled, fail early when `@modelcontextprotocol/sdk` is missing, and add `silica mcp-key` for generating API keys.
