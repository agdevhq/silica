---
"@silicajs/core": patch
"@silicajs/next": patch
---

Use frontmatter descriptions only in the page UI, resolve meta tags from manual or generated plain-text blurbs, and strip markdown formatting from both through a shared cleaner. Fix the monorepo docs site dev scripts to call the CLI `bin.js` entrypoint after the published entrypoint split.
