{
  "name": "silica-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Silica framework monorepo",
  "type": "module",
  "packageManager": "npm@10.9.7",
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "build": "turbo run build",
    "changeset": "changeset",
    "dev": "turbo watch build --filter=docs^... & turbo run dev --filter=docs",
    "e2e": "npm run build && playwright test",
    "lint": "turbo run lint",
    "release:check": "npm run scaffold-versions:check && npm run format && npm run build && npm run lint && npm run typecheck && npm test",
    "release:publish": "changeset publish",
    "release:version": "changeset version && npm run scaffold-versions:update",
    "scaffold-versions:check": "node scripts/sync-scaffold-versions.mjs --check",
    "scaffold-versions:update": "node scripts/sync-scaffold-versions.mjs --write",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@playwright/test": "^1.60.0",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^25.9.1",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "prettier": "^3.7.3",
    "tsup": "^8.5.1",
    "turbo": "^2.9.16",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
