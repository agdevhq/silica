# create-silica

Reserved scaffolder for the upcoming [Silica](https://www.npmjs.com/package/silicajs) framework — a Next.js framework for publishing Obsidian-flavored markdown vaults.

Once the first real release ships, run:

```bash
npx create-silica my-vault
```

This `0.0.0` release is a placeholder reserving the name.
