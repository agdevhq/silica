# create-silica

`npx create-silica <dir>` wrapper.

Delegates to `@silicajs/cli`'s `silica create` command to scaffold a new vault project with content, config, env template, Dockerfile, and GitHub Actions workflow.
