# Minimal Silica Vault

Dogfood fixture for the Silica framework.

```bash
npm run build
npm run dev
```

The visible project is intentionally only markdown and config. The Next.js app is materialized under `.silica/next/`.
