---
---

Bootstrap Changesets release automation without bumping package versions.

All public packages are still unpublished, so the first publish should use the
current `0.1.0` package versions. Future package changes should use normal
package-scoped changesets.
